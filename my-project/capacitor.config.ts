import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rmktech.cap',
  appName: 'my-project',
  webDir: 'dist/capacitor',
  bundledWebRuntime: false
};

export default config;
